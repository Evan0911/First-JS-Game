/*const nameInput = document.createElement('input')
nameInput.type = "text"
const submitNameButton = document.createElement('button')
submitNameButton.innerText = "Valider"
submitNameButton.addEventListener('click', SubmitPlayerName)

textDiv.appendChild(nameInput)
actionsDiv.appendChild(submitNameButton)
StartBattle()
function SubmitPlayerName()
{
    if(nameInput.value != "")
    {
        playerName = nameInput.value
        StartBattle(name)
        nameInput.remove()
        submitNameButton.remove()
    }
}*/